<%= form_for (@deck) do |f| %>
<% if deck.errors.any? %>
 <div id="error_explanation">
   <h2><%= pluralize(deck.errors.count, "error") %> prohibited this deck from being saved:</h2>

   <ul>
     <% deck.errors.full_messages.each do |message| %>
       <li><%= message %></li>
     <% end %>
   </ul>
 </div>
 <% end %>

 <%= f.label :name, class: "devise" %>
 <%= f.text_field :name %>


 <%= f.submit "Create Deck", class: "button expanded"%>
<% end %>
